<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign Up - MyKap ERP</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            background-color: #eef2f6;
        }
        .register-card {
            max-width: 420px;
            width: 100%;
            border: none;
            border-radius: 1rem;
            box-shadow: 0 0.5rem 1.5rem rgba(0,0,0,0.1);
        }
    </style>
</head>
<body>
    <div class="card register-card">
        <div class="card-body p-4 p-md-5">
            <div class="text-center mb-4">
                <img src="/images/logo-full.png" alt="MyKap Logo" style="width: 150px; margin-bottom: 1rem;">
                
                <h2 class="fw-bold">Create Your Account</h2>
                <p class="text-muted">Get started by creating your account.</p>
            </div>
            <form action="/register" method="POST">
                <div class="mb-3">
                    <label for="email" class="form-label">Email</label>
                    <input type="email" id="email" name="email" class="form-control" required>
                </div>
                <div class="mb-3">
                    <label for="password" class="form-label">Password</label>
                    <input type="password" id="password" name="password" class="form-control" required>
                </div>
                <button type="submit" class="btn btn-primary w-100 btn-lg">Sign Up</button>
            </form>
            <div class="text-center mt-4">
                <p class="text-muted">Already have an account? <a href="/login">Sign In</a></p>
            </div>
        </div>
    </div>
</body>
</html>