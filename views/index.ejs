<%- include('partials/header', { pageTitle: 'Dashboard' }) %>

<div class="container-fluid">
    <div class="mb-4">
        <h1 class="h2 fw-bold">Welcome back, <% if (currentUser) { %><%= currentUser.firstName %><% } %>!</h1>
        <p class="text-muted">Select a module to start working.</p>
    </div>

    <div class="row">
        <div class="col-lg-6 mb-4">
            <div class="card h-100 border-0 shadow-sm">
                <div class="card-body p-4 d-flex flex-column">
                    <h4 class="card-title fw-bold">Prospects</h4>
                    <p class="card-text">Manage and track all loan applications in process.</p>
                    <a href="/prospects" class="btn btn-primary mt-auto">Go to Prospects <i class="bi bi-arrow-right"></i></a>
                </div>
            </div>
        </div>

        <div class="col-lg-6 mb-4">
            <div class="card h-100 border-0 shadow-sm">
                <div class="card-body p-4 d-flex flex-column">
                    <h4 class="card-title fw-bold">Lenders</h4>
                    <p class="card-text">View and manage capital providers and their portfolio loans.</p>
                    <a href="/lenders" class="btn btn-primary mt-auto">Go to Lenders <i class="bi bi-arrow-right"></i></a>
                </div>
            </div>
        </div>

        </div>
</div>

<%- include('partials/footer') %>